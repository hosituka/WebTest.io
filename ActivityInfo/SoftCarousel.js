document.addEventListener("DOMContentLoaded", () => {
    const SoftCarouselContainer = document.querySelector(".SoftCaroselContaine");
    const SoftCarousel = document.querySelector(".SoftCarousel");
    const SoftCard = document.querySelectorAll(".SoftCard");
    if (!SoftCarouselContainer) return;
});
